/**
 * Debounce value
 */
export type DebounceValue = string | number;

/**
 * Debounce value delay (in miliseconds, default 500ms)
 * @example
 * ```ts
 * 250 // 0.25 second
 * 500 // 0.5 second
 * 1000 // 1 second
 * ```
 */
export type DebounceDelay = number;
